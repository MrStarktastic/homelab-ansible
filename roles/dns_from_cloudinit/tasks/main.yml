---
- name: Read cloud-init ENI file
  stat:
    path: /etc/network/interfaces.d/50-cloud-init
  register: eni

- name: Extract nameservers from 'lo' stanza in ENI
  shell: |
    awk '
      $0 ~ /^iface[[:space:]]+lo[[:space:]]+inet/ { inlo=1; next }
      $0 ~ /^iface[[:space:]]+/ { inlo=0 }
      inlo && $1=="dns-nameservers" { for (i=2;i<=NF;i++) print $i }
    ' /etc/network/interfaces.d/50-cloud-init | tr '\n' ' '
  register: lo_ns
  changed_when: false
  when: eni.stat.exists

- set_fact:
    _dns_nameservers: "{{ lo_ns.stdout.split() if (eni.stat.exists and (lo_ns.stdout | trim) != '') else [] }}"

- name: Fallback to default gateway if no nameservers found
  shell: "ip -4 route show default | awk '{print $3}' | head -n1"
  register: gw
  changed_when: false
  when: _dns_nameservers | length == 0

- set_fact:
    _dns_nameservers: ["{{ gw.stdout }}"]
  when: _dns_nameservers | length == 0 and (gw.stdout | trim) != ""

- assert:
    that: _dns_nameservers | length > 0
    fail_msg: "No DNS nameserver found."
    success_msg: "Using DNS servers: {{ _dns_nameservers | join(', ') }}"

- name: Write /etc/resolv.conf from effective values
  template:
    src: resolv.conf.j2
    dest: /etc/resolv.conf
    mode: "0644"
